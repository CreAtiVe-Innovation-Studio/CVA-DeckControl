# Vorlage fuer config/ha_secrets.yaml (die echte Datei ist per .gitignore
# ausgeschlossen). Kopieren, umbenennen zu ha_secrets.yaml, eigene Werte
# eintragen: cp config/ha_secrets.example.yaml config/ha_secrets.yaml
#
# token: Home Assistant -> Profil -> unten "Langlebige Zugangs-Token" ->
# neuen Token erstellen.
url: "http://homeassistant.local:8123"
token: "DEIN-LANGLEBIGER-HOME-ASSISTANT-TOKEN"
